# --- Web framework ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# --- Database ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- LLM (LOCAL OpenAI-compatible clients only: Ollama, vLLM, LM Studio...;
# the app never calls cloud AI APIs) ---
openai>=1.30

# --- Vector store ---
chromadb>=0.5.5,<2.0

# --- Document extraction ---
PyMuPDF>=1.24.5
python-docx>=1.1.0
Pillow>=10.3.0

# --- OCR ---
pytesseract>=0.3.10
# Pure-Python fallback OCR when Tesseract is not installed on the host.
# ONNX models are bundled in the wheel - fully offline, no system install.
rapidocr-onnxruntime>=1.3.20

# --- Sandbox ---
psutil>=5.9

# --- Sample document generation ---
reportlab>=4.0

# --- Testing ---
pytest>=8.0
httpx>=0.27